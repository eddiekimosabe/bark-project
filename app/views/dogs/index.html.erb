<p>Sort by: <%= link_to "Like", root_path(order: "likes") %></p>

<div class="row no-gutters"><%= render partial: 'thumbnail', collection: @dogs, as: :dog %></div>

<%= will_paginate @dogs, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer%>

